{
  "name": "venturelift",
  "version": "1.0.0",
  "description": "VentureLift entrepreneurial support platform with optional Supabase sync.",
  "type": "module",
  "main": "server.mjs",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test tests/test_backend.mjs tests/test_unit.mjs",
    "test:python": "python -m pytest tests/ -v",
    "test:all": "npm test && npm run test:python"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.0.0"
  }
}
